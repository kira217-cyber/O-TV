import React from "react";
import ComingSoon from "../../components/ComingSoon/ComingSoon";

const NormalUsers = () => {
  return (
    <ComingSoon
      title="Normal Users"
      description="Client-site viewer accounts aren't built yet — once the O-TV client site has its own user login, they'll be managed here."
    />
  );
};

export default NormalUsers;
