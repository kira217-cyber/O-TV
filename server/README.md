# O-TV - Server

Express + MongoDB backend for O-TV.

## Setup

```
npm install
```

Copy `.env` and set your own `MONGO_URI` and `JWT_SECRET`.

## Run

```
npm run dev
```

Server runs on `http://localhost:5000` by default.
